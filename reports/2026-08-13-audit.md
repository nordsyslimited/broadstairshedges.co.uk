# Morning audit — 2026-08-13

**Run date:** 2026-08-13  
**Session scope:** Auto-fix pass (em-dash elimination) + report-only review  
**Files changed:** 31 (including robots.txt and sitemap.xml from prior session work)

---

## Auto-fixed

### Em-dash elimination (U+2014 → `: ` or `, `)

All em-dashes removed from every served file sitewide. Rules applied:
- `<title>`, `<h1>`, `<h2>`, JSON-LD strings, after `</strong>` in term-definition patterns → `: `
- Regular prose sentences, parenthetical pairs → `, `

Files cleaned this audit cycle:

| File | Em-dashes fixed |
|---|---|
| index.html | 3 (FAQ JSON-LD, two prose sentences) |
| services/index.html | 3 (meta, two card paragraphs) |
| services/hedge-cutting.html | 1 (prose) |
| services/hedge-reduction.html | 10 (lede, species list, 4× prose) |
| services/hedge-planting.html | 1 (`<title>` on redirect/noindex page) |
| services/tree/index.html | 8 (h1, h2, 4× cards, TPO section) |
| services/tree/pruning.html | 12 (h1, meta, species list, 4× prose) |
| services/tree/crown-reduction.html | 11 (h1, h2, meta, species list, prose) |
| services/tree/crown-raising.html | 13 (h1, meta, species list, 4× prose) |
| services/tree/crown-thinning.html | 10 (h1, meta, species list, 3× prose) |
| services/tree/deadwooding.html | 11 (h1, meta, JSON-LD, species notes, 4× prose) |
| how-to/herring-gull-nesting-cliff-hedges-broadstairs.html | 3 (prose) |
| how-to/coastal-hedges-broadstairs-species-guide.html | 1 (prose) |
| how-to/hedge-planting-north-foreland-chalk-exposure.html | 1 (prose) |
| how-to/dickens-festival-holiday-let-hedge-tidy-broadstairs.html | 5 (lede, 4× list items) |
| jobs/index.html | 1 (placeholder text) |
| jobs/jobs.json | 1 (rendered job summary) |
| assets/partials.html | 1 (HTML comment — internal only) |

Files cleaned in previous session(s) this audit cycle:

- areas/index.html ✓
- how-to/when-to-cut-hedges-broadstairs-calendar.html ✓
- how-to/hedge-reduction-broadstairs-clifftop.html ✓
- how-to/high-hedges-neighbour-law-broadstairs.html ✓
- how-to/index.html ✓
- how-to/broadstairs-conservation-areas-hedge-rules.html ✓
- about.html ✓
- robots.txt ✓ (comment line; also received AI-crawler Allow blocks)
- sitemap.xml ✓
- 404.html ✓
- thanks.html ✓
- privacy.html ✓
- contact.html ✓

**Final verification:** `grep -r "—" **/*.html` → 0 matches across all HTML. `jobs/jobs.json` also clean.

---

### robots.txt — AI crawler Allow blocks (prior session)

Added `Allow` entries for 15 AI/search crawlers including GPTBot, ClaudeBot, PerplexityBot, Amazonbot, Bytespider, and others. No `Disallow` rules were present or added.

### sitemap.xml — lastmod values (prior session)

Checked and updated where `<lastmod>` values were missing. Sitemap now covers all current pages.

---

## Needs review (flagged, not touched)

### 1. TEMPLATES/how-to.md contains em-dashes throughout

`TEMPLATES/how-to.md` uses em-dashes extensively in section headings and examples (e.g., `"Step 1 — ..."`, `"Step 2 — ..."`). This template instructs the ingestion routine to write `h3` sub-steps with em-dashes, which would reintroduce the prohibited character. **Recommend updating the template** to use `: ` in these patterns before the next ingestion run.

### 2. AGENTS.md, INGESTION.md, README.md contain em-dashes

All internal developer documentation. Not served as HTML. These are editorial inconsistencies rather than SEO/user-facing issues. Low priority but worth a sweep if the team wants internal consistency.

### 3. No how-to articles added since scaffold

The ingestion scaffold was committed 2026-08-12. As of this audit, no new how-to articles have been generated via the nightly ingestion pipeline. `config/ingestion.json` shows `nightly` and `morning` trigger IDs as `"TBD"`. The pipeline needs its scheduled triggers created on claude.ai/code to begin producing content.

### 4. jobs.json has only 3 entries

All dated July 2026. The "recent jobs" feed is live but sparse. No action needed from the audit; this is operational, not structural.

### 5. WCA 1981 nesting callout coverage

The nesting-season callout (WCA 1981, 1 March–31 August) is correctly present in:
- `how-to/herring-gull-nesting-cliff-hedges-broadstairs.html` (primary article)
- `how-to/when-to-cut-hedges-broadstairs-calendar.html` (timing article)

Articles without a callout (correct, as they have no timing dimension):
- `how-to/broadstairs-conservation-areas-hedge-rules.html` — no timing element, callout not needed
- `how-to/coastal-hedges-broadstairs-species-guide.html` — species selection, not timing
- `how-to/hedge-planting-north-foreland-chalk-exposure.html` — site-preparation, not timing
- `how-to/dickens-festival-holiday-let-hedge-tidy-broadstairs.html` — seasonal but the article's own body addresses timing; callout would duplicate

No gaps identified. Coverage looks correct.

### 6. Structural-angle consistency

All existing how-to articles are procedural-first (numbered steps or action-led sections appearing early in the body). No drift detected. The TEMPLATES/how-to.md contract is correctly capturing this pattern.

### 7. Orphan/duplicate checks

No orphan pages found. All pages listed in sitemap.xml are reachable from the nav or from linked hub pages. No duplicates detected.

### 8. Broken internal links

Not audited programmatically this run. Manual spot-check of nav and footer links found no obvious breakage. Recommend a full link-check tool pass when pipeline is operational.

---

## Summary

- **Em-dash prohibition:** Fully enforced across all served HTML and JSON. Zero remaining matches.
- **robots.txt:** AI crawlers allowed.
- **sitemap.xml:** Current.
- **No new content:** Ingestion pipeline triggers not yet created; nightly article generation has not started.
- **Highest priority follow-up:** Update `TEMPLATES/how-to.md` to remove em-dash patterns before first ingestion run.
