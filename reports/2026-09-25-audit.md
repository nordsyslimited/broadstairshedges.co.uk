# Morning audit: broadstairshedges.co.uk — 25 September 2026

Automated site health audit. Fired 2026-09-25 08:12 UTC.

---

## Summary

No auto-fixes required today. All check categories are clean. Six carry-over items remain in the needs-review queue (unchanged since last audit). The new article shipped by today's content run (`ivy-in-hedges-management-broadstairs.html`) passes all audit checks.

---

## Check results

### 1. How-to inventory

47 articles on the filesystem, 47 linked from `how-to/index.html`. No orphans, no duplicates.

### 2. Internal links

0 broken relative hrefs across all HTML pages.

### 3. Video embeds

No YouTube `<iframe>` or `<embed>` elements found. YouTube references in articles are text citations in the sources footnote only — no live embeds to probe.

### 4. SEO basics

All pages checked across root, how-to, services, areas. Every page carries:
- Unique `<title>`
- `<meta name="description">`
- `<link rel="canonical">`
- `<meta name="theme-color" content="#1e2f45">`

No gaps.

### 5. JSON-LD

All 47 how-to articles use the correct flat single `Article` object. No article page uses the `@graph`/`BreadcrumbList` pattern. Hub page (`how-to/index.html`) correctly uses `@graph` with `CollectionPage` + `BreadcrumbList`.

One carry-over: `broadstairs-conservation-areas-hedge-rules.html` uses expanded formatting (multi-line) rather than the compact one-liner house style. Functionally identical; carry-over, flagged below.

### 6. Sitemap

Sitemap contains all 47 how-to articles, all service pages (excluding `services/hedge-planting.html` and `services/hedge-removal.html`, which are noindex client-side redirect stubs and correctly omitted), `how-to/index.html`, `areas/`, `jobs/`, `about.html`, `contact.html`, `privacy.html`. All entries have `lastmod` dates. Complete.

### 7. Robots.txt

All 15 required AI crawlers present: GPTBot, ChatGPT-User, OAI-SearchBot, PerplexityBot, ClaudeBot, Claude-Web, Google-Extended, CCBot, Applebot, Applebot-Extended, Bingbot, Amazonbot, DuckAssistBot, Meta-ExternalAgent, YouBot. Sitemap directive present. Clean.

### 8. Tone

- Em-dashes: 0 found across all HTML files.
- Banned filler (utilise, leverage, seamlessly, best-in-class, synergy, robust, cutting-edge): 0 found.
- Non-UK spellings: none found in article prose (CSS class names use `center` for layout, not article text).

### 9. WCA 1981 legal callout

All 31 cutting/trimming/timing articles checked for Wildlife and Countryside Act 1981 nesting-season reference (1 March-31 August). All pass. Two carry-over items on non-cutting articles flagged below.

### 10. Content relevance

No off-topic articles detected. All 47 articles address hedge and tree care topics relevant to Broadstairs/CT10.

### 11. Structural angle

Checked recent articles (ivy-in-hedges, hedge-propagation-hardwood-cuttings, blackthorn-hedge-pruning). All open with `<h2>Steps</h2>` immediately after the `<h1>` and lede, before any scene-setting sections. Procedural-first structure confirmed.

---

## Auto-fixed

**None.** No auto-fix items were triggered today.

---

## Needs review (carry-over, 6 items)

1. **`broadstairs-conservation-areas-hedge-rules.html`: JSON-LD formatting.** The JSON-LD block uses expanded multi-line format rather than the compact single-line style used by all other articles. Functionally valid; cosmetic inconsistency. Content/template decision.

2. **`broadstairs-conservation-areas-hedge-rules.html`: no WCA 1981 nesting callout.** The article covers s.211 conservation area tree law. It does not include a callout on the Wildlife and Countryside Act 1981 nesting-season window. This article's topic (conservation area paperwork) is borderline for the callout: it does reference timing for hedge works indirectly. Content decision.

3. **`coastal-hedges-broadstairs-species-guide.html`: timing section lacks explicit WCA 1981 s.1 callout.** The species guide discusses optimal planting and establishment timing but does not include a formal `<div class="callout">` with the 1 March-31 August window. Content decision.

4. **`hedge-planting-north-foreland-chalk-exposure.html`: no WCA 1981 callout.** Planting article; may legitimately be exempt if no cutting/timing element. Content decision.

5. **YouTube citations unverifiable via proxy.** 36 articles cite YouTube videos in their sources footnotes; YouTube egress is blocked from the generation environment (see `INGESTION.md`). All were verified at generation time via web-search cross-reference. This is an infrastructure constraint, not a content defect. Videos have not been proxy-checked since the environment cannot reach YouTube. No action required unless the team wants a manual spot-check pass.

6. **Redirect stubs `services/hedge-planting.html` and `services/hedge-removal.html`.** Both are client-side HTML redirects (noindex,follow, `<meta http-equiv="refresh">`) pointing to `/services/`. Server-side 301 redirects configured at the hosting level would be cleaner and avoid the brief client-visible redirect. Carry-over from PR #7 (2026-07-17). Infra/hosting decision.

---

## New article check — ivy-in-hedges-management-broadstairs.html

Published 2026-09-25 by today's content run. Audit pass:

| Check | Status |
|---|---|
| Procedural-first (`<h2>Steps</h2>` before scene-setting) | Pass |
| WCA 1981 callout in body | Pass (callout at Step 2, extended to September for ivy/robin nesting context) |
| JSON-LD: flat `Article` object | Pass |
| In sitemap with lastmod 2026-09-25 | Pass |
| Linked from `how-to/index.html` | Pass |
| theme-color, canonical, meta description, robots | Pass |
| No em-dashes, no filler | Pass |
| UK English | Pass |
